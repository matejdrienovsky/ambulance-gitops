apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- 'https://github.com/matejdrienovsky/ambulance-webapi//deployments/kustomize/install' # ?ref=v1.0.1
- http-route.yaml

patches:
- path: patches/ambulance-webapi.deployment.yaml

labels:
- pairs:
    app.kubernetes.io/component: xdrienovskym2-ambulance-webapi
